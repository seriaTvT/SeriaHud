<!--
  SeriaHud launcher mark: a HUD "screen" frame with a performance waveform,
  echoing the app's signature frametime graph. Drawn in white so it works as
  the adaptive-icon foreground (white on pink); the same drawable is used as the
  <monochrome> layer, which Android 13+ recolors for themed icons. Placeholder
  mark — swap for finished artwork when available.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- HUD screen frame (kept within the adaptive-icon safe circle) -->
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="5"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M38,36 h32 a8,8 0 0 1 8,8 v20 a8,8 0 0 1 -8,8 h-32 a8,8 0 0 1 -8,-8 v-20 a8,8 0 0 1 8,-8 z" />
    <!-- Performance waveform -->
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="5"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M37,60 L45,51 L52,57 L59,46 L66,56 L71,50" />
</vector>
